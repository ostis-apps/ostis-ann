action_interpret_pooling_layer
=> nrel_main_idtf:
    [действие интерпретации пулинг слоя](* <- lang_ru;; *);
    [action of interpretation the pooling layer](* <- lang_en;; *);
<- rrel_key_sc_element:
    ...
    (*
    => nrel_main_idtf:
        [Опр. (действие интерпретации пулинг слоя)]
        (* <- lang_ru;; *);
        [Def. (action of interpretation the pooling layer)]
        (* <- lang_en;; *);;
    <- definition;;
    => nrel_sc_text_translation:
        {
        rrel_example:
            [действие интерпретации пулинг слоя — действие интерпретации слоя и.н.с., в ходе которого реализуется определенный метод, связанный с интерпретацией пулинг слоя и.н.с.]
            (* <- lang_ru;; *)
        };;
    => nrel_using_constants:
        {
        action_interpret_pooling_layer;
        action_interpret_ann_layer;
        layer;
        concept_artificial_neural_network
        };;
    *);
=> nrel_arguments_of_action: ...
(*
    -> rrel_input_matrix;;
    -> rrel_pooling_window_size;;
    -> rrel_pooling_window_step;;
    -> rrel_result;;
*);;