class MarkingModel:

    def __init__(self, cap_detected, marking_detected, marking, marking_image):
        self.cap_detected = cap_detected
        self.marking_detected = marking_detected
        self.marking = marking
        self.marking_image = marking_image
